// formatMap.js
import {
  formatArray,
  formatDateArray,
  formatDate,
} from "./formatItems";

export const FORMATTER = {
  array: formatArray,
  date: formatDate,
  dateArray: formatDateArray,
};
